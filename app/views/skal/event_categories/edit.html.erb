<h2 class="ui header">
  <i class="write icon"></i>
  編集：<%= @event_category.name %>
</h2>

<div class="ui breadcrumb">
  <%= link_to raw('<i class="home icon"></i>'), skal_root_path(), class: 'section' %>
  <i class="right chevron icon divider"></i>
  <%= link_to raw("イベントカテゴリ一覧"), event_categories_path(), class: 'section' %>
  <i class="right chevron icon divider"></i>
  <div class="section"><%= @event_category.name %></div>
</div>

<%= simple_form_for(@event_category, url: event_category_path(@event_category)) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>